{
  "name": "setup-fortran",
  "version": "2.0.0",
  "description": "Set up Fortran compilers on GitHub Actions runners",
  "homepage": "https://github.com/fortran-lang/setup-fortran#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fortran-lang/setup-fortran.git"
  },
  "bugs": {
    "url": "https://github.com/fortran-lang/setup-fortran/issues"
  },
  "license": "Apache-2.0",
  "author": {
    "name": "Fortran Community",
    "url": "https://fortran-lang.org"
  },
  "type": "commonjs",
  "main": "index.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map",
    "build:integration-test": "ncc build src/run_fortran_tests.ts -o dist-tests --source-map",
    "build:verify-installation": "ncc build src/verify_installation.ts -o dist-verify --source-map",
    "format": "prettier --write src/**/*.ts",
    "format:check": "prettier --check src/**/*.ts",
    "lint": "eslint src/**/*.ts",
    "test": "jest",
    "test:bundle": "node -e \"process.env.SETUP_FORTRAN_BUNDLE_SMOKE_TEST='1'; require('./dist/index.js')\"",
    "integration-test": "node dist-tests/index.js",
    "verify-installation": "node dist-verify/index.js",
    "all": "npm run format && npm run lint && npm run test && npm run build && npm run test:bundle"
  },
  "dependencies": {
    "@actions/cache": "^5.2.0",
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/tool-cache": "^4.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.2.0",
    "@vercel/ncc": "^0.45.0",
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "jest": "^30.4.2",
    "js-yaml": "^5.3.0",
    "prettier": "^3.9.6",
    "ts-jest": "^29.4.12",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0"
  }
}
